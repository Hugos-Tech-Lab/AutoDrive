package example:device;

interface hardware {
    control-wheel: func(wheel: f32, speed: f32);
    read-wheel: func() -> f32;
}

world app {
    import hardware: hardware;
    export prepare: func();
    // export start: func();
    // export pause: func();
    // export cancel: func();
    export run: func();
}
