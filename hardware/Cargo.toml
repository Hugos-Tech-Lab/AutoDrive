[workspace]
members = [
    "common",
    # "the-beginner",
    "weather-station-v0",
]

resolver = "1"

[workspace.dependencies]

[profile.release]
opt-level = "s"

[profile.dev]
debug = true    # Symbols are nice, and they don't increase the size on Flash
opt-level = "z"
